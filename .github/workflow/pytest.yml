name: Tests
on: [push]
jobs:

  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: lint-and-format
        run: echo 'lint-and-format'

  run-tests:
    name: python
    runs-on: ubuntu-latest
    needs: [lint-and-format]
    steps:
      - name: Run tests
        run: echo 'run-tests'

  mobile-app-tests:
    name: mobile-app
    runs-on: ubuntu-latest
    needs: [lint-and-format]
    steps:
      - name: Run tests
        run: echo 'mobile-app-tests'

